# USO Enterprise Platform — Backend dependencies
# All packages are free / open-source (MIT, BSD, Apache, LGPL).

fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
pydantic-settings>=2.1
python-jose[cryptography]>=3.3      # JWT encode/decode
passlib[argon2]>=1.7               # password hashing (Argon2id)
argon2-cffi>=23.1
python-multipart>=0.0.9            # multipart/form-data (file upload)
email-validator>=2.1
openpyxl>=3.1                      # CPM .xlsx parsing
psycopg2-binary>=2.9              # PostgreSQL driver (prod)

# Dev / test
pytest>=8.0
httpx>=0.27
